using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WallDirectionCheck : MonoBehaviour
{
    // Start is called before the first frame update
    public bool Front;
    public bool Flip;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
